kind = "web"
previewPath = "/"
title = "Coincidence"
version = "1.0.0"
id = "artifacts/coincidence"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26246

[services.development]
run = "pnpm --filter @workspace/coincidence run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/coincidence", "run", "build" ]
publicDir = "artifacts/coincidence/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26246"
BASE_PATH = "/"
